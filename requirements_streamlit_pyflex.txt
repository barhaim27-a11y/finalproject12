# ---- Core runtime ----
streamlit>=1.34

# Numpy / Pandas / SciPy (handle Python 3.13+ vs older)
numpy>=2.1 ; python_version >= "3.13"
numpy>=1.26 ; python_version < "3.13"
pandas>=2.2 ; python_version >= "3.13"
pandas>=2.1 ; python_version < "3.13"
scipy>=1.13

# ML stack
scikit-learn>=1.6    ; python_version >= "3.13"
scikit-learn>=1.3    ; python_version < "3.13"
imbalanced-learn>=0.12
xgboost>=2.0
joblib>=1.3
matplotlib>=3.8

# Optional: enable the 'KerasNN' model
 scikeras>=0.13
 tensorflow-cpu>=2.15   ; platform_system != "Darwin"
 tensorflow-macos>=2.15 ; platform_system == "Darwin" and platform_machine == "arm64"

# Optional (images), not required by the app logic
 pillow>=10
